const initialState = {
	selected: '',
	checked: '',
	text: ''
}

export default function manageComponents(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }	
}
